# 자체 보안대책

## 1. 사업 개요

- 사업명: 화성오산 초등학교 통학구역 조회 웹앱 구축 및 운영
- 서비스 방식: Cloudflare Pages 기반 정적 웹서비스
- 서비스 대상: 일반 이용자, 학부모, 업무 참고자
- 처리 정보: 공개 통학구역 및 주소 검색용 정적 데이터
- 개인정보 수집: 없음

## 2. 보안 기본방침

- 서비스는 정적 웹앱으로 구성하여 서버, DB, 로그인, 관리자 기능을 두지 않는다.
- 이용자가 입력한 주소 또는 학교명은 브라우저 내 검색 처리에만 사용하고 서버에 저장하지 않는다.
- 배포 데이터는 공개 가능한 자료만 포함한다.
- GitHub 및 Cloudflare 접근 권한은 업무상 필요한 담당자에게만 부여한다.
- 배포 전 보안 설정, 데이터 파일, 기능 동작을 점검한다.

## 3. 관리적 보안대책

### 3.1 보안관리 수행체계

| 구분 | 담당 | 역할 |
| --- | --- | --- |
| 정보화사업 총괄책임자 | 작성 필요 | 사업 총괄 및 보안대책 이행 감독 |
| 정보보안담당관 | 작성 필요 | 보안성 검토, 보안대책 검토, 사고 대응 총괄 |
| 시스템 운영 담당자 | 작성 필요 | GitHub/Cloudflare 운영, 배포 및 데이터 갱신 |
| 데이터 관리 담당자 | 작성 필요 | 원본 자료 검토, JSON 데이터 생성 및 갱신 |

### 3.2 계정 및 권한 관리

- GitHub 및 Cloudflare 계정은 개인별 계정을 사용한다.
- 공동 계정 사용을 지양한다.
- GitHub 및 Cloudflare 계정에 MFA를 적용한다.
- GitHub 저장소 접근 권한은 최소 권한 원칙에 따라 부여한다.
- Cloudflare Pages 프로젝트 접근 권한은 필요한 담당자에게만 부여한다.
- 담당자 변경 또는 업무 종료 시 GitHub/Cloudflare 권한을 즉시 회수한다.

### 3.3 변경 및 배포 관리

- 운영 배포 브랜치는 `main`으로 지정한다.
- 중요한 변경은 별도 브랜치에서 검토 후 `main`에 병합한다.
- 배포 전 아래 명령을 실행한다.

```bash
npm run check
npm run smoke
npm run audit:security
npm run audit:free-tier
```

- 데이터 갱신 시 원본 자료 출처, 기준일, 생성일, 변경자를 기록한다.
- Cloudflare Pages 배포 로그를 확인하여 실패 배포 여부를 점검한다.

### 3.4 사고 대응

보안사고 또는 오배포 발생 시 다음 절차를 따른다.

1. 서비스 이상 여부 확인
2. Cloudflare Pages 배포 로그 확인
3. 최근 배포 버전 확인
4. 필요 시 Cloudflare Pages의 이전 정상 배포로 롤백
5. 영향 범위 확인
6. 정보보안담당관 및 관련 부서에 보고
7. 원인 분석 및 재발 방지 조치

## 4. 물리적 보안대책

본 서비스는 별도 서버 장비를 기관 내부에 설치하지 않고 Cloudflare Pages를 이용한다.

- 기관 내 물리 서버, 네트워크 장비, DB 서버를 운영하지 않음
- 원본 자료를 보관하는 담당자 PC는 기관 보안정책에 따라 관리
- 담당자 PC에는 최신 보안 업데이트 및 백신 적용
- 원본 자료 저장매체는 비인가자가 접근하지 못하도록 관리
- 담당자 변경 시 원본 자료 및 배포 권한 인계인수 확인

## 5. 기술적 보안대책

### 5.1 웹사이트 보안

- 정적 파일만 배포하고 서버 사이드 실행 코드를 운영하지 않는다.
- 외부 스크립트를 사용하지 않는다.
- 외부 API를 호출하지 않는다.
- HTML, CSS, JavaScript, JSON 파일은 동일 출처에서만 제공한다.
- 사용자 입력값은 HTML 출력 시 이스케이프 처리한다.
- 파일 업로드, 게시판, 로그인, 관리자 페이지를 제공하지 않는다.
- 배포 전 JavaScript 문법 검사 및 스모크 테스트를 수행한다.

### 5.2 보안 HTTP 헤더

`public/_headers`에 다음 보안 헤더를 적용한다.

- `Strict-Transport-Security`
- `X-Frame-Options: DENY`
- `X-Content-Type-Options: nosniff`
- `Referrer-Policy: no-referrer`
- `Permissions-Policy`
- `Cross-Origin-Opener-Policy`
- `Cross-Origin-Resource-Policy`
- `Content-Security-Policy`

주요 효과:

- 클릭재킹 방지
- MIME 스니핑 방지
- 외부 스크립트 실행 제한
- iframe 삽입 차단
- 브라우저 권한 기능 차단
- referrer 정보 전송 최소화

### 5.3 데이터 보안

- 배포 데이터는 공개 가능한 통학구역/주소 검색 데이터만 포함한다.
- 개인정보, 인증정보, 내부망 정보, IP 현황, 계정정보를 포함하지 않는다.
- 원본 자료는 `source_data/`에 보관하고 GitHub에 커밋하지 않는다.
- 실제 배포에는 `public/data/*.json`만 포함한다.
- 데이터 갱신 시 생성 스크립트로 재생성하고 스모크 테스트를 수행한다.

### 5.4 클라우드 및 네트워크 보안

- Cloudflare Pages를 이용하여 HTTPS 기반으로 정적 파일을 배포한다.
- 기관 내부망, 업무망, 폐쇄망과 연동하지 않는다.
- 별도 방화벽 포트 개방, VPN, 서버 접속 계정을 운영하지 않는다.
- Cloudflare 계정은 MFA를 적용하고 접근 권한을 최소화한다.
- Cloudflare GitHub 앱 권한은 해당 저장소에만 허용한다.
- Cloudflare Pages Free 플랜만 사용하고 Workers, Pages Functions, KV, R2, D1 등 과금 가능 기능은 사용하지 않는다.
- 배포 전 `npm run audit:free-tier`로 Cloudflare Pages Free 한도 초과 가능성을 점검한다.

### 5.5 GitHub 저장소 보안

- 저장소 접근권한은 최소화한다.
- 가능하면 GitHub 저장소를 Private로 운영한다.
- `main` 브랜치 보호 규칙 적용을 권고한다.
- push 전 검증 명령 실행을 운영 절차로 둔다.
- 원본 자료 및 비공개 자료는 `.gitignore`로 제외한다.
- GitHub Actions는 표준 Linux runner만 사용하고 artifact, cache, larger runner는 사용하지 않는다.
- 비공개 저장소 운영 시 GitHub Actions 월 2,000분 무료 한도 사용량을 정기 확인한다.

## 6. 재난복구 및 백업 대책

- GitHub 저장소를 코드 및 배포 데이터의 기준 백업으로 활용한다.
- Cloudflare Pages는 과거 배포 이력을 제공하므로 장애 시 이전 정상 배포로 롤백한다.
- 원본 자료는 담당 부서 관리 저장소 또는 기관 보안정책에 맞는 위치에 별도 보관한다.
- 데이터 갱신 전후 생성 파일과 원본 자료 기준일을 기록한다.

## 7. 용역업체 및 외부인력 보안대책

외부 용역 또는 외부 개발자가 참여하는 경우 다음을 적용한다.

- 보안서약서 징구
- 작업 범위 및 접근 권한 명시
- GitHub/Cloudflare 접근권한 최소 부여
- 작업 종료 시 권한 회수
- 원본 자료 반출 금지
- 비공개 자료, 계정정보, 토큰, 내부 문서 제공 금지
- 작업 결과물 검토 후 반영
- 필요 시 작업 이력 및 커밋 이력 보관

## 8. 누출금지정보 관리

다음 정보는 저장소와 배포 파일에 포함하지 않는다.

- 기관 내부 IP주소 및 네트워크 구성도
- GitHub/Cloudflare 계정 비밀번호
- API 토큰, 배포 토큰, 인증키
- 비공개 내부 문서
- 개인정보
- 보안취약점 점검 결과 원문
- 원본 자료 중 공개 승인되지 않은 자료

## 9. 배포 전 보안 점검

배포 전 `04_배포_운영_보안점검표.md`를 확인한다.
