# 정보화사업 개요

## 1. 사업명

화성오산 초등학교 통학구역 조회 웹앱 구축 및 운영

## 2. 사업 목적

화성오산 지역 초등학교 통학구역 및 통리반 정보를 일반 이용자가 주소 또는 학교명으로 간편하게 조회할 수 있도록 정적 웹서비스를 구축한다.

## 3. 사업 기간

- 구축 기간: `YYYY.MM.DD ~ YYYY.MM.DD`
- 운영 기간: `YYYY.MM.DD ~ 별도 종료 시점`

## 4. 사업 유형

해당 가능성이 있는 유형은 다음과 같다.

- 홈페이지 및 웹기반 정보시스템 구축
- 민간 클라우드컴퓨팅 서비스 이용
- 일반 이용자 대상 대민 정보 제공 서비스

최종 검토기관 및 보안성 검토 필요 여부는 소속 학교 정보보안담당관 및 교육지원청 정보보안담당관 검토에 따른다.

## 5. 서비스 대상

- 학부모
- 일반 시민
- 학교 및 교육지원청 업무 참고자

## 6. 주요 기능

- 주소 조회
  - 도로명주소, 지번주소, 아파트명, 건물명 기반 검색
  - 해당 주소 기준 배정 초등학교 및 통리반 정보 제공

- 학교명 조회
  - 초등학교명 기반 검색
  - 해당 학교 통학구역 및 통리반 정보 제공

- 자동완성
  - 주소 추천어
  - 학교명 추천어

- 다크모드/라이트모드
  - 이용자 브라우저 내 `localStorage`에 테마 설정만 저장

## 7. 시스템 구성

```text
이용자 브라우저
  |
  | HTTPS
  v
Cloudflare Pages
  |
  +-- /index.html
  +-- /style.css
  +-- /main.js
  +-- /data/core.json
  +-- /data/roads.json
  +-- /data/suggestions.json
```

## 8. 시스템 특성

- 정적 웹사이트 형태로 운영
- 별도 서버 애플리케이션 없음
- DBMS 없음
- 회원가입 없음
- 로그인 없음
- 관리자 페이지 없음
- 게시판 없음
- 파일 업로드 없음
- 결제 기능 없음
- 외부 API 호출 없음
- 사용자 입력값 서버 저장 없음

## 9. 사용 기술

- HTML
- CSS
- JavaScript
- Cloudflare Pages
- GitHub 저장소 연동 배포
- GitHub Actions 자동 점검

## 10. 개인정보 포함 여부

현재 서비스는 이용자의 개인정보를 수집, 저장, 처리하지 않는다.

- 주소 입력값: 브라우저 내 검색 처리에만 사용하며 서버에 저장하지 않음
- 쿠키: 사용하지 않음
- localStorage: 다크모드/라이트모드 설정만 저장
- 회원정보: 없음
- 접속자 식별정보: 애플리케이션 코드에서 수집하지 않음

단, Cloudflare 서비스 운영 과정에서 발생하는 접속 로그 또는 보안 로그는 Cloudflare 서비스 정책 및 기관 운영 설정에 따라 별도 검토가 필요하다.

## 11. 데이터 구성

배포 데이터는 아래 정적 JSON 파일로 구성된다.

- `public/data/core.json`
  - 통리반 및 초등학교 통학구역 검색용 데이터

- `public/data/roads.json`
  - 화성오산 지역 도로명주소-지번주소 검색용 데이터

- `public/data/suggestions.json`
  - 주소/학교명 자동완성 추천어 데이터

원본 자료는 `source_data/`에 보관하되 GitHub 저장소에는 커밋하지 않는다.

## 12. 연계 시스템

없음.

현재 웹앱은 외부 시스템, 내부망, 업무망, 교육행정 시스템, 인증 시스템과 연계하지 않는다.

## 13. 네트워크 구성

- 인터넷 공개 서비스
- Cloudflare Pages를 통한 정적 파일 배포
- 내부망 또는 폐쇄망과 연동하지 않음
- 기관 내부 시스템과 직접 연결하지 않음

## 14. 주요 보안대책 요약

- HTTPS 기반 서비스 제공
- 보안 HTTP 응답 헤더 적용
- Content Security Policy 적용
- 프레임 삽입 차단
- 브라우저 권한 기능 차단
- 외부 스크립트 및 외부 API 미사용
- 사용자 입력값 미저장
- GitHub/Cloudflare 계정 MFA 적용 권고
- 배포 브랜치 보호 및 권한 최소화 권고
- 배포 전 `npm run check`, `npm run smoke` 검증 수행
- 배포 전 `npm run audit:security`, `npm run audit:free-tier` 검증 수행
- Cloudflare Pages Free 플랜 범위에서 정적 파일만 운영
- Workers, Pages Functions, KV, R2, D1 미사용
